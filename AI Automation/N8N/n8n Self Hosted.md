**Install :**
- On prem (locally machine and house) (*more complex*) but **FREE**
	- linux based server
	- Docker
- Cloud (less comples) but *Paid*
- Download Docker
- Install Docker
- Launch Docker > Images > n8nio > Pull and run
- Optional settings > Host port 5678




// For local Self hosted setup

> **MISC**

- Multi trigger action (e.g. Gmail and Chat trigger)
	- use **SET** node
	- SET nodes should be used in parallel
	- then combine both set note with another consolidated **SET** node