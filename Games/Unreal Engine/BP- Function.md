- A block of code which adds functionality to the program
- Adding a Function switches view to New Function BP graph
- *MyFunction* will be available in the *Event Graph*
- Values can be passed to MyFunction from variables 
- Input and Output parameters can be added to *MyFunction*
- variable as input needs to be added in the Event Graph
- Adding *Output* to the Function will add a **Return Node** in the Function Graph
- Output can have empty return node as well
- **Local Variables**
	- Only exists within the function
- `Delay` cannot be implemented inside a Function
- Cannot have Latent node inside a BP
- 

![[myFunction.png]]